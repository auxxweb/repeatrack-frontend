# Optional: when the app is built and hosted separately from the API, set the API
# origin (no trailing slash). Dev server continues to use the Vite /api proxy if unset.
VITE_API_URL=http://localhost:5000
VITE_API_URL=https://server.repeatrack.auxxbay.com
